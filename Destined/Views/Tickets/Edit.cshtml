@model Destined.Models.Ticket
@{
    ViewData["Title"] = "Редактиране на билет";
    ViewData["FullWidth"] = true;
}

<div style="position: relative; min-height: 100vh; overflow-x: hidden;">
    <partial name="_ThemeBackground" />

    <div id="page-content" style="position: relative; z-index: 10; padding: 40px 20px;">
        <div class="ticket-wrapper">
            <h1 class="page-title">Редактиране на билет</h1>

            <form asp-action="Edit" method="post">
                <input type="hidden" asp-for="Id" />

                <div class="color-controls">
                    <label>Цвят лява част: <input asp-for="LeftColor" type="color" id="leftColorPicker" /></label>
                    <label>Цвят дясна част: <input asp-for="RightColor" type="color" id="rightColorPicker" /></label>
                    <label>Цвят на текста: <input asp-for="TextColor" type="color" id="textColorPicker" /></label>
                </div>

                <div class="real-ticket">
                    <div class="ticket-left" id="ticketLeft" style="background:@Model.LeftColor; color:@Model.TextColor;">
                        <h2>
                            <input asp-for="From" class="edit-input location-input" placeholder="От" maxlength="22" />
                            <span class="arrow">➔</span>
                            <input asp-for="To" class="edit-input location-input" placeholder="До" maxlength="22" required />
                        </h2>
                        <p class="date-time">
                            <strong>Дата и час:</strong>
                            <input asp-for="DepartureTime" type="datetime-local" class="edit-input" required />
                            <span asp-validation-for="DepartureTime" class="text-danger"></span>
                        </p>
                        <p class="passengers">
                            <strong>Пътници:</strong>
                            <input asp-for="NumberOfPassengers" type="number" min="1" class="edit-input small-input" required />
                            <span asp-validation-for="NumberOfPassengers" class="text-danger"></span>
                        </p>
                        
                        <div class="dropdown-container">
                            <p style="margin:0;">
                                 <strong>Държава:</strong>
                            </p>
                            <input type="text" id="countrySearch" class="dropdown-search edit-input" placeholder="Търси държава..." autocomplete="off" value="@Model.Country">
                            <input asp-for="Country" type="hidden" id="selectedCountry" />
                            <span asp-validation-for="Country" class="text-danger"></span>
                            <div id="countryList" class="dropdown-list">
                                <!-- Countries will be populated here -->
                            </div>
                        </div>
                        <div class="form-group form-check" style="margin-top: 15px;">
                            <input asp-for="IsPublic" class="form-check-input" type="checkbox" />
                            <label asp-for="IsPublic" class="form-check-label">Публичен билет?</label>
                            <span asp-validation-for="IsPublic" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="ticket-center-wrapper">
                        <div class="ticket-center">
                            <div class="perforation"></div>
                        </div>
                    </div>

                    <div class="ticket-right" id="ticketRight" style="background:@Model.RightColor; color:@Model.TextColor;">
                        <div class="barcode"></div>
                        <div class="serial-number">ID: @Model.Id</div>
                        <div class="actions">
                            <button type="submit" class="btn btn-edit">Запази</button>
                            <a asp-action="Index" class="btn btn-delete">Отказ</a>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const maxLength = 22;
            const inputs = document.querySelectorAll('input[asp-for="From"], input[asp-for="To"]');

            inputs.forEach(input => {
                input.addEventListener('input', () => {
                    if (input.value.length > maxLength) {
                        input.value = input.value.slice(0, maxLength);
                    }
                });
            });

            // Country Dropdown Logic
            const countries = [
                // Европа
                "Албания","Андора","Австрия","Беларус","Белгия","Босна и Херцеговина","България",
                "Ватикана","Великобритания","Германия","Гърция","Дания","Естония","Ирландия",
                "Исландия","Испания","Италия","Кипър","Латвия","Литва","Лихтенщайн","Люксембург",
                "Малта","Молдова","Монако","Нидерландия","Норвегия","Полша","Португалия",
                "Румъния","Сан Марино","Северна Македония","Словакия","Словения","Сърбия",
                "Украйна","Унгария","Финландия","Франция","Хърватия","Черна гора","Чехия",
                "Швейцария","Швеция","Русия","Косово",

                // Азия
                "Афганистан","Армения","Азербайджан","Бангладеш","Бахрейн","Бруней","Бутан",
                "Виетнам","Грузия","Израел","Индия","Индонезия","Ирак","Иран","Йордания",
                "Казахстан","Камбоджа","Катар","Киргизстан","Китай","Кувейт","Лаос","Ливан",
                "Малайзия","Малдиви","Монголия","Мианмар","Непал","ОАЕ","Оман","Пакистан",
                "Саудитска Арабия","Северна Корея","Сингапур","Сирия","Таджикистан","Тайланд",
                "Туркменистан","Турция","Узбекистан","Филипини","Шри Ланка","Южна Корея",
                "Япония","Палестина","Тимор-Лесте","Йемен",

                // Африка
                "Алжир","Ангола","Бенин","Ботсвана","Буркина Фасо","Бурунди","Габон","Гамбия",
                "Гана","Гвинея","Гвинея-Бисау","Джибути","Египет","Екваториална Гвинея",
                "Еритрея","Есватини","Етиопия","Замбия","Зимбабве","Кабо Верде","Камерун",
                "Кения","Коморски острови","Конго","ДР Конго","Кот д’Ивоар","Лесото","Либерия",
                "Либия","Мадагаскар","Малави","Мали","Мароко","Мавритания","Мавриций",
                "Мозамбик","Намибия","Нигер","Нигерия","Руанда","Сао Томе и Принсипи",
                "Сейшели","Сенегал","Сиера Леоне","Сомалия","Судан","Южен Судан","Танзания",
                "Того","Тунис","Уганда","Централноафриканска република","Чад","ЮАР",

                // Северна и Централна Америка
                "Антигуа и Барбуда","Бахами","Барбадос","Белиз","Гватемала","Доминика",
                "Доминиканска република","Канада","Коста Рика","Куба","Мексико","Никарагуа",
                "Панама","Салвадор","САЩ","Сейнт Китс и Невис","Сейнт Лусия",
                "Сейнт Винсент и Гренадини","Тринидад и Тобаго","Хаити","Хондурас","Ямайка",
                "Гренада",

                // Южна Америка
                "Аржентина","Боливия","Бразилия","Венецуела","Гаяна","Колумбия","Парагвай",
                "Перу","Суринам","Уругвай","Чили","Еквадор",

                // Океания
                "Австралия","Вануату","Кирибати","Маршалови острови","Микронезия","Науру",
                "Нова Зеландия","Палау","Папуа Нова Гвинея","Самоа","Соломонови острови",
                "Тонга","Тувалу","Фиджи"
            ]; 

            const searchInput = document.getElementById('countrySearch');
            const countryList = document.getElementById('countryList');
            const hiddenInput = document.getElementById('selectedCountry');

            // Move to body
            document.body.appendChild(countryList);

            function updateDropdownPosition() {
                const rect = searchInput.getBoundingClientRect();
                const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
                const scrollLeft = window.pageXOffset || document.documentElement.scrollLeft;
                
                countryList.style.top = (rect.bottom + scrollTop) + 'px';
                countryList.style.left = (rect.left + scrollLeft) + 'px';
                countryList.style.width = rect.width + 'px';
            }

            function populateList(filter = "") {
                countryList.innerHTML = '';
                const filtered = countries.filter(c => c.toLowerCase().includes(filter.toLowerCase()));
                
                // Show all if filter is empty
                let showList = filter === "" ? countries : filtered;

                if (showList.length === 0) {
                    const noResult = document.createElement('div');
                    noResult.className = 'dropdown-item';
                    noResult.textContent = 'Няма резултати';
                    
                    noResult.style.color = '#999';
                    noResult.style.cursor = 'default';
                    countryList.appendChild(noResult);
                    return;
                }

                showList.forEach(country => {
                    const item = document.createElement('div');
                    item.className = 'dropdown-item';
                    item.textContent = country;
                    item.addEventListener('click', () => {
                        searchInput.value = country;
                        hiddenInput.value = country;
                        countryList.style.display = 'none';
                    });
                    countryList.appendChild(item);
                });
            }

            searchInput.addEventListener('input', () => {
                populateList(searchInput.value);
                countryList.style.display = 'block';
                updateDropdownPosition();
            });

            searchInput.addEventListener('focus', () => {
                populateList(searchInput.value);
                countryList.style.display = 'block';
                updateDropdownPosition();
            });

            window.addEventListener('resize', () => {
                if (countryList.style.display === 'block') updateDropdownPosition();
            });
            window.addEventListener('scroll', () => {
                 if (countryList.style.display === 'block') updateDropdownPosition();
            }, true);

            document.addEventListener('click', (e) => {
                if (!searchInput.contains(e.target) && !countryList.contains(e.target)) {
                    countryList.style.display = 'none';
                }
            });

            const leftPicker = document.getElementById('leftColorPicker');
            const rightPicker = document.getElementById('rightColorPicker');
            const textPicker = document.getElementById('textColorPicker');

            const ticketLeft = document.getElementById('ticketLeft');
            const ticketRight = document.getElementById('ticketRight');

            const arrow = ticketLeft.querySelector('.arrow');
            const dateTimeLabel = ticketLeft.querySelector('.date-time strong');
            const passengersLabel = ticketLeft.querySelector('.passengers strong');

            const buttonTextColor = "#ffffff";

            let currentTextColor = textPicker.value;
            let currentLeftColor = leftPicker.value;
            let currentRightColor = rightPicker.value;

            function applyTextColor() {
                const leftInputs = ticketLeft.querySelectorAll('input.edit-input');
                const leftTextElements = [...leftInputs, arrow, dateTimeLabel, passengersLabel];
                leftTextElements.forEach(el => {
                    el.style.transition = "none";
                    el.style.color = currentTextColor;
                    if (el.tagName === 'INPUT') {
                        el.style.borderColor = currentTextColor;
                        el.style.setProperty('--placeholder-color', currentTextColor);
                    }
                });

                const rightTextElements = ticketRight.querySelectorAll('*:not(.serial-number):not(.actions)');
                rightTextElements.forEach(el => {
                    if (el.tagName !== 'BUTTON' && el.tagName !== 'A' && el.tagName !== 'INPUT') {
                        el.style.transition = "none";
                        el.style.color = currentTextColor;
                    }
                });

                ticketRight.querySelectorAll('.actions button, .actions a').forEach(btn => {
                    btn.style.transition = "none";
                    btn.style.color = buttonTextColor;
                });
            }

            leftPicker.addEventListener('input', () => {
                currentLeftColor = leftPicker.value;
                ticketLeft.style.backgroundColor = currentLeftColor;
                applyTextColor();
            });

            rightPicker.addEventListener('input', () => {
                currentRightColor = rightPicker.value;
                ticketRight.style.backgroundColor = currentRightColor;
                applyTextColor();
            });

            textPicker.addEventListener('input', () => {
                currentTextColor = textPicker.value;
                applyTextColor();
            });

            ticketLeft.style.backgroundColor = currentLeftColor;
            ticketRight.style.backgroundColor = currentRightColor;
            applyTextColor();
        });
    </script>
}

<style>
    .ticket-wrapper {
        max-width: 1000px;
        margin: 0 auto;
        padding: 0 20px;
    }

    .page-title {
        text-align: center;
        font-size: 2.5rem;
        margin-bottom: 30px;
        color: #fff; /* White for dark themes */
        font-weight: 700;
        letter-spacing: 1.2px;
        text-shadow: 0 2px 4px rgba(0,0,0,0.3);
    }
    
    .color-controls {
        background: rgba(255,255,255,0.1);
        backdrop-filter: blur(10px);
        padding: 15px;
        border-radius: 10px;
        margin-bottom: 20px;
        display: flex;
        justify-content: center;
        gap: 20px;
        border: 1px solid rgba(255,255,255,0.2);
    }

    .color-controls label {
        margin-right: 0;
        font-weight: 600;
        color: white;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .real-ticket {
        display: flex;
        background: linear-gradient(145deg, #fafafa, #e6e6e6);
        border-radius: 15px;
        margin-bottom: 30px;
        filter: drop-shadow(0 10px 10px rgba(0,0,0,0.15));
        cursor: default;
        transition: transform 0.3s ease, filter 0.3s ease;
        border: 1.5px solid #bbb;
        position: relative;
        font-weight: 600;
        user-select: none;
        
        -webkit-mask: 
            radial-gradient(circle at center top, transparent 15px, black 15.5px) top center / 100% 51% no-repeat,
            radial-gradient(circle at center bottom, transparent 15px, black 15.5px) bottom center / 100% 51% no-repeat;
        mask: 
            radial-gradient(circle at center top, transparent 15px, black 15.5px) top center / 100% 51% no-repeat,
            radial-gradient(circle at center bottom, transparent 15px, black 15.5px) bottom center / 100% 51% no-repeat;
    }

    .ticket-left, .ticket-right {
        padding: 25px 30px;
        flex: 1;
        position: relative;
        z-index: 10;
    }

    .ticket-left {
        background: #e0f2f1;
        border-right: 1.5px dashed #999;
        display: flex;
        flex-direction: column;
        justify-content: center;
        border-radius: 15px 0 0 15px; /* Radius fix */
    }

    /* Custom Dropdown Styles */
    .dropdown-container {
        position: relative;
        width: 100%;
        margin-bottom: 10px;
        margin-top: 10px;
    }

    .dropdown-search {
        width: 100%;
        margin-bottom: 5px;
        background: transparent;
        border: none;
        border-bottom: 1.5px dashed #004d40;
        color: #004d40;
        font-weight: 600;
        font-size: 1.1rem;
        padding: 6px 4px;
    }

    .dropdown-search::placeholder {
        color: #00796b88;
    }

    .dropdown-search:focus {
        outline: none;
        border-color: #4caf50;
    }

    .dropdown-list {
        position: absolute;
        background: white;
        border: 1px solid #ccc;
        border-radius: 4px;
        max-height: 220px;
        overflow-y: auto;
        z-index: 10000;
        display: none;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .dropdown-item {
        padding: 8px 12px;
        cursor: pointer;
        transition: background 0.2s;
        color: #333; /* Ensure text is visible in dropdown */
        text-shadow: none; /* No shadow in dropdown */
    }

    .dropdown-item:hover {
        background: #e0f2f1;
        color: #00796b;
    }


        .ticket-left h2 {
            font-size: 2rem;
            margin-bottom: 12px;
            color: #00796b;
            text-shadow: 0 1px 1px #a0d4cf;
            letter-spacing: 1.2px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .ticket-left p {
            font-size: 1.1rem;
            color: #004d40;
            margin: 6px 0;
            display: flex;
            flex-direction: column;
        }

        .ticket-left h2 .arrow {
            font-size: 2rem;
            transition: color 0.3s ease;
        }

    .edit-input {
        background: transparent;
        border: none;
        border-bottom: 1.5px dashed #004d40;
        color: #004d40;
        font-weight: 600;
        font-size: 1.1rem;
        padding: 6px 4px;
        transition: border-color 0.3s ease, color 0.3s ease;
        max-width: 100%;
    }

        .edit-input:focus {
            outline: none;
            border-color: #4caf50;
        }

    .location-input {
        width: 100%;
    }

    .small-input {
        max-width: 150px;
        font-size: 1.1rem;
    }



    .ticket-center-wrapper {
        width: 30px;
        overflow: hidden;
        position: relative;
    }

    .ticket-center {
        width: 30px;
        height: 100%;
        background: repeating-linear-gradient(to bottom, transparent, transparent 8px, #999 8px, #999 10px);
        position: relative;
    }
    
    /* Notches removed */

    .ticket-right {
        background: #fff;
        box-shadow: inset 0 4px 10px rgba(0,0,0,0.1);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        border-left: 1.5px dashed #999;
        border-radius: 0 15px 15px 0; /* Radius fix */
    }

    .barcode {
        width: 100%;
        height: 60px;
        background: repeating-linear-gradient(to right, #000, #000 3px, #fff 3px, #fff 6px, #000 6px, #000 9px, #fff 9px, #fff 12px, #000 12px, #000 15px, #fff 15px, #fff 18px);
        border-radius: 8px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.25);
        margin-bottom: 18px;
    }

    .serial-number {
        font-size: 0.9rem;
        font-family: 'Courier New', Courier, monospace;
        color: #666;
        margin-bottom: 15px;
        text-align: center;
        user-select: text;
    }

    .actions {
        display: flex;
        justify-content: space-around;
        padding-bottom: 10px;
    }

    .btn {
        padding: 10px 22px;
        border-radius: 25px;
        text-decoration: none;
        font-size: 0.95rem;
        font-weight: 700;
        cursor: pointer;
        transition: background 0.3s ease, transform 0.2s ease;
        box-shadow: 0 3px 8px rgba(0,0,0,0.15);
        user-select: none;
        border: none;
    }

    .btn-edit {
        background: #ff9800;
        color: white;
    }

        .btn-edit:hover {
            background: #ef6c00;
            transform: translateY(-3px);
        }

    .btn-delete {
        background: #f44336;
        color: white;
    }

        .btn-delete:hover {
            background: #d32f2f;
            transform: translateY(-3px);
        }
</style>
